// An example of O(n) algorithm
// An example of O(n^2) algorithm
// An example of O(n^3) algorithm
// An example of O(1) algorithm
function getMiddle (array: number[]): number {
    return array[Math.floor(array.length / 2)];
}